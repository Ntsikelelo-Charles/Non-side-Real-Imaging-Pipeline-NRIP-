
_include:
    library:
    
       recipes:
          - flag.yml
          - inspect.yml
          - transform.yml
          
       steps:
          - inspect.yml
          - imaging.yml
          - quartical.yml
          - prepdata.yml
          - flagdata.yml
          - quartical.yml
          - calibration.yml
           
       params:
          - imaging.yml
          - flagging.yml
          - quartical.yml
          - calibration.yml
          - information.yml
          - manipulation.yml
          - visualization.yml
           
    (cultcargo):
       
       - crystalball.yml
       - tricolour.yml
       - breizorro.yml
       - quartical.yml
       - wsclean.yml
       - rfinder.yml
       - shadems.yml
       - casa/flag.yml
       - casa/setjy.yml
       - casa/polcal.yml
       - casa/plotms.yml
       - casa/listobs.yml
       - casa/bandpass.yml
       - casa/plotants.yml
       - casa/clearcal.yml 
       - casa/calibration.yml
       - casa/mstransform.yml
       - casa/flag.yml ## different name
       - smops.yml
       - taql.yml
       - msutils.yml
       - fitstool.yml
       - bdsf.yml
       - tigger-convert.yml ## different name
       - tricolour.yml 
      


cabs:


  crystalball:
    inputs:
      num-workers:
        dtype: int
        default: 4
 
                
  mv:
    command: mv
    policies:
        positional: true
    inputs:
        src:
          dtype: File
          required: true
    outputs:  
        dest:
          dtype: Union[File, Directory]
          required: true
          
  cp:
    command: cp -a
    policies:
        positional: true
    inputs:
        src:
          dtype: File
          required: true
    outputs:
        dest:
          dtype: Union[File, Directory]
          required: true
    
  prepkat:
    command: katflip
    backend:
        select: native
        native:
    inputs:
        ms_path: 
          dtype: Directory 
          required: true 
          policies:
              positional: true
        column: 
          dtype: 'str' 
  mkdir:
      command: mkdir
      inputs:
         dtype: str
         policies:
            positional: true  
 

  fix-ms-direction:
    info: gets direction of given solar system body for mid-point of MS
    inputs:
      ms:
        dtype: MS
        required: true
        writable: true
      body:
        dtype: str
        required: true
    flavour: python-code
    command: |
      import sys
      import casacore.measures
      import casacore.quanta 
      from casacore.tables import table
      dm = casacore.measures.measures() 
      dq = casacore.quanta
      newdir = dm.direction(body)
      print(ms)
      tab = table(ms, readonly=False)
      t = tab.getcol("TIME")
      t0 = (t.min() + t.max())/2
      ep = dm.epoch('utc', dq.quantity(t0, 's'))
      dm.do_frame(ep)
      radec = dm.measure(newdir, 'J2000')
      ra = dm.get_value(radec)[0].get_value('rad')
      dec = dm.get_value(radec)[1].get_value('rad')
      print(f"time {t0} {body} ra {ra} dec {dec}")
      ftab = table(f"{ms}::FIELD", readonly=False)
      for colname in 'DELAY_DIR', 'PHASE_DIR', 'REFERENCE_DIR':
          col = ftab.getcol(colname)
          print(col)
          col[0,0,0] = ra
          col[0,0,1] = dec
          ftab.putcol(colname, col)
          print(colname, col)

  casa.fixuvw:
    flavour: casa-task
    command: fixvis
    inputs:
      ms: 
        dtype: MS
        required: true
        nom_de_guerre: vis
      reuse:
        dtype: bool
        implicit: false
    outputs:
      outputvis:
        implicit: '{current.ms}'

  casa.split.scan:
    command: split
    flavour: casa-task
    inputs:
      source-ms:
        dtype: MS
        required: true
        nom_de_guerre: vis
      scan:
        dtype: str
      datacolumn:
        default: corrected  
    outputs:
      ms:
        dtype: MS
        required: true
        nom_de_guerre: outputvis



  save-ms-info:
    flavour: python-code
    name: save-ms-info
    command: |
      from omegaconf import OmegaConf
      import casacore.tables
      tab = casacore.tables.table(ms)
      info = OmegaConf.create()
      info.num_intervals = len(set(tab.getcol("TIME")))
      info.intervals = sorted(map(float, set(tab.getcol("INTERVAL"))))
      print(f"MS has {info.num_intervals} timeslots of interval {info.intervals}")
      info.total = [i*info.num_intervals/3600. for i in info.intervals]
      print(f"Total synthesis time is {info.total}h")
      OmegaConf.save(info, ms_info_file)
    inputs:
      ms: 
        dtype: MS
    outputs:
      ms-info-file:
        dtype: File

  load-ms-info:
    flavour: python-code
    command: |
      from omegaconf import OmegaConf
      info = OmegaConf.load(ms_info_file)
      num_intervals = info.num_intervals
    inputs:
      ms-info-file: 
        dtype: File
    outputs:
      num_intervals:
        dtype: int
     


lib:
  recipes:
    fft-image-list:
      inputs:
        image-list:
          dtype: List[File]
          required: true
      for_loop:
        var: image
        over: image-list
      steps:
        fft-image:
          cab: fft-image
          params:
            image: =recipe.image
            fft-image: =STRIPEXT(current.image) + ".fft.fits"
            lambda-per-arcsec: 100

  steps:    
    wsclean:
      base:
        info: "base wsclean settings"
        cab: wsclean
        params:
          ms: ='{recipe.msdir}/target.ms'
          prefix: ="{recipe.imdir}/image_{info.suffix}/image_{info.suffix}"
          column: CORRECTED{info.suffix}_DATA
          size: =recipe.wsclean_size
          scale: =recipe.wsclean_scale
          padding: 1.3
          wstack-nwlayers-factor: 3
          nchan: =recipe.wsclean_nchan
          temp-dir: ='{recipe.msdir}/tmp'
          use-wgridder: true
          log-time: true
          # minuv-l: =IF(recipe.inner-taper.enable, recipe.inner-taper.cut, UNSET)
          # taper-inner-tukey: =IF(recipe.inner-taper.enable, recipe.inner-taper.taper, UNSET)
      
   
      dirty:
        _use: lib.steps.wsclean.base
        info: "wsclean step for dirty-only image"
        params:
          column: CORRECTED_DATA
          weight: 'briggs 0'
          niter: 0
          # parallel_gridding: 64

      dirty_pol:
        _use: lib.steps.wsclean.dirty
        info: "wsclean step for dirty-only IQUV image"
        params:
          pol: IQUV

      image:
        _use: lib.steps.wsclean.dirty
        info: "wsclean step for imaging"
        params:
          column: CORRECTED_DATA
          mgain: 0.9
          weight: =recipe.wsclean_weight
          fit-beam: true
          elliptical-beam: true
          parallel-deconvolution: 1500
          niter: 10000
          join-channels: true
          fit-spectral-pol: 4
          
          
          

      image_pol:
        _use: lib.steps.wsclean.image
        info: "wsclean step for IQUV imaging"
        params:
          join-polarizations: true
          fit-spectral-pol: =UNSET 
          pol: IQUV
          auto-mask : =UNSET
          auto-threshold: 3
        
      image_pol_interval_out:
        _use: lib.steps.wsclean.image_pol
        info: "wsclean step for IQUV imaging"
        params:
          multiscale: true
          intervals-out: 5
          auto-threshold: 3

      image_pol2:
        _use: lib.steps.wsclean.image_pol
        info: "wsclean step for IQ imaging"
        params:
          pol: IQ

      predict:
        _use: lib.steps.wsclean.base
        info: "wsclean step for predicting a model"
        params:
          predict: true

      predict_pol:
        _use: lib.steps.wsclean.predict
        info: "wsclean step for predicting an IQUV model"
        params:
          predict: true
          pol: IQUV

      predict_pol2:
        _use: lib.steps.wsclean.predict_pol
        info: "wsclean step for predicting an IQ model"
        params:
          pol: IQ

  